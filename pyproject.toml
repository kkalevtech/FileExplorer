[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "file_explorer"
version = "0.1.0"
description = "Core backend for a File Explorer application"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
